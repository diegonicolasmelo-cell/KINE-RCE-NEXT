# Dependencias y artefactos generados: nunca se versionan.
build/node_modules/
build/servicios.gs
build/index_cohete.html
build/package-lock.json
build/package.json
build/sim/sim_compilado.html
build/entrega_*/
*.zip

# Notas de trabajo que no son del proyecto.
scratchpad/
HANDOFF.md
build/pantallazos/
# Sondas de un rato: se escriben dentro de build/ porque ahí vive
# playwright-core, y no son del proyecto.
build/_*
build/*_tmp.js
